<h1>New User</h1>

<% semantic_form_for [:admin, @user] do |f| %>
  <%= render(:partial => "form", :locals => { :f => f }) %>
  <p><%= f.submit "Create User" %> or <%= link_to "Back to Users", admin_users_path %></p>
<% end %>